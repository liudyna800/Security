-- =============================================
-- SECURITY V5 — FLEE THE FACILITY (Чистый дизайн + Koala Core)
-- Исправлено: тогглы с шариком, правильное размещение, рабочие функции
-- =============================================

local TweenService = game:GetService("TweenService")
local KHLib = loadstring(game:HttpGet("https://pastebin.com/raw/BkRLnxZW"))():getKHLib("v2.0.x")
KHLib:Initialize()

local function IsThereChar() 
    local plr = game.Players.LocalPlayer
    return plr.Character and plr.Character:FindFirstChild("Humanoid") 
end

-- ==================== SECURITY V5 GUI ====================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SecurityV5_FTF"
ScreenGui.Parent = game:GetService("CoreGui")

local MainFrame = Instance.new("Frame")
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 20)
MainFrame.Size = UDim2.new(0, 340, 0, 280)
MainFrame.Position = UDim2.new(0.5, -170, 0.5, -140)
MainFrame.Active = true
MainFrame.Draggable = true

Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)
Instance.new("UIStroke", MainFrame).Color = Color3.fromRGB(40, 40, 70)

-- Header
local Header = Instance.new("Frame")
Header.Parent = MainFrame
Header.BackgroundColor3 = Color3.fromRGB(22, 22, 35)
Header.Size = UDim2.new(1, 0, 0, 42)

Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 12)

local accent = Instance.new("Frame")
accent.Parent = MainFrame
accent.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
accent.Size = UDim2.new(1, 0, 0, 2)
accent.Position = UDim2.new(0, 0, 0, 74)

local gradient = Instance.new("UIGradient")
gradient.Color = ColorSequence.new(Color3.fromRGB(0,150,255), Color3.fromRGB(150,0,255))
gradient.Parent = accent

local title = Instance.new("TextLabel")
title.Parent = Header
title.Text = "SECURITY V5"
title.Size = UDim2.new(1, -50, 1, 0)
title.Position = UDim2.new(0, 45, 0, 0)
title.BackgroundTransparency = 1
title.TextColor3 = Color3.fromRGB(230, 230, 240)
title.TextSize = 16
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left

local subtitle = Instance.new("TextLabel")
subtitle.Parent = Header
subtitle.Text = "Flee The Facility"
subtitle.Size = UDim2.new(1, -50, 1, 0)
subtitle.Position = UDim2.new(0, 45, 0, 22)
subtitle.BackgroundTransparency = 1
subtitle.TextColor3 = Color3.fromRGB(120, 120, 150)
subtitle.TextSize = 10
subtitle.Font = Enum.Font.Gotham

-- Tab Bar
local tabBar = Instance.new("Frame")
tabBar.Parent = MainFrame
tabBar.BackgroundColor3 = Color3.fromRGB(18, 18, 30)
tabBar.Size = UDim2.new(1, 0, 0, 30)
tabBar.Position = UDim2.new(0, 0, 0, 44)

local tabNames = {"Player", "ESP", "Misc"}
local tabBtns = {}
local pages = {}

for i, name in ipairs(tabNames) do
    local btn = Instance.new("TextButton")
    btn.Parent = tabBar
    btn.Text = name
    btn.Size = UDim2.new(0, 106, 1, 0)
    btn.Position = UDim2.new(0, (i-1)*113 + 5, 0, 0)
    btn.BackgroundColor3 = Color3.fromRGB(18, 18, 30)
    btn.TextColor3 = Color3.fromRGB(130, 130, 160)
    btn.TextSize = 12
    btn.Font = Enum.Font.GothamBold
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    tabBtns[i] = btn
end

local function makePage()
    local p = Instance.new("Frame")
    p.Parent = MainFrame
    p.BackgroundTransparency = 1
    p.Size = UDim2.new(1, 0, 1, -78)
    p.Position = UDim2.new(0, 0, 0, 78)
    p.Visible = false
    return p
end

for i = 1, 3 do
    pages[i] = makePage()
end
pages[1].Visible = true

-- Tab switching
local function switchTab(i)
    for k, p in ipairs(pages) do p.Visible = (k == i) end
    for k, b in ipairs(tabBtns) do
        b.BackgroundColor3 = (k == i) and Color3.fromRGB(30,30,55) or Color3.fromRGB(18,18,30)
        b.TextColor3 = (k == i) and Color3.fromRGB(0,170,255) or Color3.fromRGB(130,130,160)
    end
end

for i, btn in ipairs(tabBtns) do
    btn.MouseButton1Click:Connect(function() switchTab(i) end)
end

-- ==================== КРАСИВЫЕ ТОГГЛЫ С ШАРИКОМ ====================
local function CreateToggle(parent, text, yPos, default, callback)
    local toggleFrame = Instance.new("Frame")
    toggleFrame.Parent = parent
    toggleFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
    toggleFrame.Size = UDim2.new(0.9, 0, 0, 36)
    toggleFrame.Position = UDim2.new(0.05, 0, 0, yPos)
    Instance.new("UICorner", toggleFrame).CornerRadius = UDim.new(0, 18)

    local label = Instance.new("TextLabel")
    label.Parent = toggleFrame
    label.Text = text
    label.Size = UDim2.new(0.7, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.TextColor3 = Color3.fromRGB(230, 230, 240)
    label.TextSize = 13
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Position = UDim2.new(0, 12, 0, 0)

    local switch = Instance.new("Frame")
    switch.Parent = toggleFrame
    switch.Size = UDim2.new(0, 52, 0, 26)
    switch.Position = UDim2.new(1, -64, 0.5, -13)
    switch.BackgroundColor3 = default and Color3.fromRGB(0, 170, 80) or Color3.fromRGB(60, 60, 70)
    Instance.new("UICorner", switch).CornerRadius = UDim.new(1, 0)

    local ball = Instance.new("Frame")
    ball.Parent = switch
    ball.Size = UDim2.new(0, 22, 0, 22)
    ball.Position = default and UDim2.new(1, -24, 0.5, -11) or UDim2.new(0, 2, 0.5, -11)
    ball.BackgroundColor3 = Color3.new(1,1,1)
    Instance.new("UICorner", ball).CornerRadius = UDim.new(1, 0)

    local enabled = default

    toggleFrame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            enabled = not enabled
            TweenService:Create(switch, TweenInfo.new(0.25), {BackgroundColor3 = enabled and Color3.fromRGB(0,170,80) or Color3.fromRGB(60,60,70)}):Play()
            TweenService:Create(ball, TweenInfo.new(0.25), {Position = enabled and UDim2.new(1, -24, 0.5, -11) or UDim2.new(0, 2, 0.5, -11)}):Play()
            callback(enabled)
        end
    end)

    return toggleFrame
end

-- ==================== PLAYER TAB ====================
local playerPage = pages[1]

CreateToggle(playerPage, "Speed Hack", 10, false, function(state)
    -- Здесь будет логика скорости
end)

local speedBox = Instance.new("TextBox")
speedBox.Parent = playerPage
speedBox.Size = UDim2.new(0.9, 0, 0, 35)
speedBox.Position = UDim2.new(0.05, 0, 0, 60)
speedBox.Text = "80"
speedBox.BackgroundColor3 = Color3.fromRGB(30,30,50)
speedBox.TextColor3 = Color3.new(1,1,1)
speedBox.TextSize = 14

CreateToggle(playerPage, "Infinite Jump", 110, false, function(state)
    -- Infinite Jump логика
end)

-- ==================== ESP TAB ====================
local espPage = pages[2]

CreateToggle(espPage, "Beast ESP", 10, false, function(state) end)
CreateToggle(espPage, "Player ESP", 55, false, function(state) end)
CreateToggle(espPage, "Exit Doors ESP", 100, false, function(state) end)
CreateToggle(espPage, "Freeze Pods ESP", 145, false, function(state) end)
CreateToggle(espPage, "Computer ESP", 190, false, function(state) end)

-- ==================== MISC TAB ====================
local miscPage = pages[3]

CreateToggle(miscPage, "Anti PC Error", 10, true, function(state) end)
CreateToggle(miscPage, "Full Bright", 55, false, function(state) end)

print("✅ Security V5 с нормальными тогглами загружен")

-- Hover для всех кнопок
for _, obj in pairs(MainFrame:GetDescendants()) do
    if obj:IsA("TextButton") or obj:IsA("TextBox") then
        local orig = obj.BackgroundColor3
        obj.MouseEnter:Connect(function()
            TweenService:Create(obj, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(orig.R*255 + 20, orig.G*255 + 20, orig.B*255 + 20)}):Play()
        end)
        obj.MouseLeave:Connect(function()
            TweenService:Create(obj, TweenInfo.new(0.2), {BackgroundColor3 = orig}):Play()
        end)
    end
end
